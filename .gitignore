node_modules/
.next/
.env
.env.*
*.tsbuildinfo
web/generated/
firebase-debug.log
firestore-debug.log
.firebase/

# Service account keys. Never commit these: a leaked key is full admin access to the project.
# The real one lives outside the repo (see docs/deploy-runbook.md).
*firebase-adminsdk*.json
*service-account*.json
